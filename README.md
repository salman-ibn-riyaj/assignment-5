Q>1.  What is the difference between var, let, and const?

answer: - using var and let we can re assign/re declare  value of a variable  but in case of const we can't redeclare or reassign value.

        - var is function scoped
        - let and const is block scoped
        - var hoisted but value is undefined
        - let and const also hoisted but can't access values

Q>2. What is the spread operator (...)?

    answer: spread operator can be used in array and object both and it bring out elements and it also copy an object or array that prevent any further change of it's reference.


Q>3. What is the difference between map(), filter(), and forEach()?

    answer: 
    - map is used in array and it is a one kind a loop and it return as an array.

    - filter is also kind a loop it bring out all the same elements and return.

    - forEach is just like map but it don't return automatically.


Q>4. What is an arrow function?

    Answer: array function is also same as traditional function but it has a different syntax such as: () => {}


Q>5. What are template literals?

    Answer: temple literals it also known as backtick  ``, in it we can use variable and can bring anythin dynamically.
        - 
